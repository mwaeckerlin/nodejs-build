{
  "name": "@mwaeckerlin/nodejs-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "docker compose build",
    "start": "docker compose up --build --force-recreate --remove-orphans",
    "stop": "docker compose down"
  }
}